"""Chunk parsed papers for retrieval. (Weekend 1-2)

Start simple (fixed-size chunks with overlap); evolve toward section-aware chunking.
"""
# TODO: split text into chunks; attach source paper + (later) section label to each.
