# AWS Deploy Runbook (manual)

Filled in during Weekend 3. Target architecture:

- **EC2** (t3.small) — Docker host running the compose stack
- **RDS Postgres** (with pgvector) — managed database
- **S3** — raw PDF storage
- **Nginx + Let's Encrypt** — HTTPS + public URL

## Steps
- [ ] TODO (Weekend 3)
